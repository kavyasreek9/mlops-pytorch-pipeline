apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: ml-training
spec:
  type: ClusterIP   # switch to LoadBalancer if on a cloud-managed cluster
  selector:
    app: model-serving
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
